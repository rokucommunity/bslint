import { Config } from './configTypes';

const defaultConfig: Config = {
    colors: {
        broadcastSafe: {
            black: '161616',
            white: 'EBEBEB'
        }
    }
};

export default defaultConfig;
